import { IParseOptions, TElevation } from './types';
declare const _default: {
    parseResort(name: string, elevation: TElevation, cb: any, opts?: IParseOptions | undefined): void;
};
export default _default;
